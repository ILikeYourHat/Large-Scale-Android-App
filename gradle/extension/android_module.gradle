apply plugin: "kotlin-android"
apply from: "$rootDir/gradle/extension/checkstyle.gradle"

android {
    compileSdk 31
    defaultConfig {
        minSdk 24
        targetSdk 31
    }
    buildFeatures {
        viewBinding false
        dataBinding false
    }
}
