apply plugin: "com.jraska.module.graph.assertion"

moduleGraphAssert {
    maxHeight = 2
    allowed = [
            'Implementation -> Contract',
            'App -> Implementation',
            'App -> Tool',
            'Implementation -> Tool',
    ]
}
