apply from: "$rootDir/gradle/module/feature_android.gradle"
apply from: "$rootDir/gradle/libs/junit5.gradle"

android {
    defaultConfig {
        consumerProguardFiles "proguard-rules.pro"
    }
}

dependencies {
    implementation libs.kotlin.reflect
}
