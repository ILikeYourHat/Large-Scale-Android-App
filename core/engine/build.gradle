apply from: "$rootDir/gradle/module/common_android.gradle"

android {
    defaultConfig {
        consumerProguardFiles "proguard-rules.pro"
    }
}

dependencies {
    api libs.bundles.koin

    implementation libs.kotlin.reflect
}
